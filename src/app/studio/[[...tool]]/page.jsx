export const dynamic = 'force-static';

export default function StudioPage() {
  return (
    <main style={{ padding: 24, fontFamily: 'system-ui, sans-serif' }}>
      <h1>Studio Disabled</h1>
      <p>
        Embedded Studio is disabled in this app build. Use the separate Studio project for content management.
      </p>
    </main>
  );
}
